import {delay} from 'redux-saga'
import { put, takeEvery, all, call } from 'redux-saga/effects'

const c = console

export function * getEntityAsync () {
	
}

export function * watchGetEntityAsync () {
	yield takeEvery('GET_ENTITY', )
}

export default function * rootSaga () {
	yield all([

	])
}